## Plano

Substituir a página inicial placeholder (`src/routes/index.tsx`) por uma página em branco minimalista exibindo apenas o texto "em branco" centralizado.

### Alterações
- `src/routes/index.tsx`: remover o placeholder e renderizar uma tela centralizada com o texto "em branco" usando tokens do design system (`bg-background`, `text-foreground`).
- Atualizar o `head()` da rota com título "Em branco".

Nenhuma outra alteração necessária.